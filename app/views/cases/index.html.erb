<h2>Cases by Description</h2>
<ul>
<% @cases.each_with_index do |cas, index| %>

  <li><%= link_to cas.description, show_case_path(index + 1) %></li>
  <% end %>
</ul>

<%= form_for @case do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.submit %>
<% end %>
